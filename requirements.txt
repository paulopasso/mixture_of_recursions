# packages for training
torch
hydra-core
omegaconf
wandb
tensorboard
transformers==4.52.4
datasets
accelerate
# CUDA-only or Linux-only packages are gated to avoid install errors on macOS/arm64
deepspeed; platform_system != "Darwin" and platform_machine != "arm64"
peft
zstandard
boto3
smart_open
# bitsandbytes and flash-attn are CUDA-only; skip on macOS
bitsandbytes; platform_system != "Darwin" and platform_machine != "arm64"
flash-attn; platform_system != "Darwin" and platform_machine != "arm64"

# FlashAttention
# pip install flash-attn --no-build-isolation


# packages for evaluation
sacrebleu
evaluate
sqlitedict
nltk
pytablewriter
rouge_score
matplotlib
more_itertools

# LM-evaluation-harness
# git clone --depth 1 https://github.com/EleutherAI/lm-evaluation-harness
# cd lm-evaluation-harness
# pip install -e .